const chai = require('chai');
const expect = chai.expect;

describe('Tests', () => {
  
  describe('Testing first test', () => {
    it('tests 1+1, expects 1', () => {
		expect(1+1).to.equal(1);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+2, expects 2', () => {
		expect(1+2).to.equal(2);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 3', () => {
		expect(1+1).to.equal(3);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 4', () => {
		expect(1+1).to.equal(4);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 5', () => {
		expect(1+1).to.equal(5);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 6', () => {
		expect(1+1).to.equal(6);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 7', () => {
		expect(1+1).to.equal(7);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 8', () => {
		expect(1+1).to.equal(8);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 9', () => {
		expect(1+1).to.equal(9);
    });
  });
  describe('Testing first test', () => {
    it('tests 1+1, expects 10', () => {
		expect(1+1).to.equal(10);
    });
  });  
});
